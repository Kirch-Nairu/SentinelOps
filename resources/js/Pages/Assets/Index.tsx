import { Head, Link } from '@inertiajs/react'; import Layout from '../../Components/Layout'; import Connectivity from '../../Components/Connectivity';
type Asset={public_id:string;code:string;name:string;status:string;revision:number;location?:{name:string}|null};
export default function Index({assets}:{assets:Asset[]}){return <Layout><Head title="Assets"/><div className="page"><div className="page-head"><div><div className="eyebrow">ASSET REGISTRY</div><h1>Field Assets</h1><p className="muted">QR-ready stable asset codes with server revisions.</p></div><Connectivity/></div><section className="panel"><div className="asset-grid">{assets.map(a=><Link className="asset-card" href={`/assets/${a.public_id}`} key={a.public_id}><div className="asset-code">{a.code}</div><h3>{a.name}</h3><p>{a.location?.name??'No location'}</p><footer><span className={`status ${a.status}`}>{a.status}</span><small>rev {a.revision}</small></footer></Link>)}</div></section></div></Layout>}
