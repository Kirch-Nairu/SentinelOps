import { Head, useForm } from '@inertiajs/react';
export default function Login(){
  const form=useForm({email:'technician@sentinelops.test',password:'SentinelOps!2026'});
  return <><Head title="Sign in"/><div className="login-page"><section className="login-card"><div className="brand large"><span className="brand-mark">S</span><div><strong>SentinelOps</strong><small>offline-first field operations</small></div></div><div className="eyebrow">AUTHORIZED ACCESS</div><h1>Operational truth, even at the edge.</h1><p className="muted">Sign in to the local pilot workspace. Offline commands remain pending until current server authority accepts them.</p><form onSubmit={e=>{e.preventDefault();form.post('/login')}}><label>Email<input type="email" value={form.data.email} onChange={e=>form.setData('email',e.target.value)} autoComplete="username"/></label><label>Password<input type="password" value={form.data.password} onChange={e=>form.setData('password',e.target.value)} autoComplete="current-password"/></label>{form.errors.email&&<p className="error">{form.errors.email}</p>}<button disabled={form.processing} className="primary">Enter SentinelOps</button></form><small className="muted">Demo accounts: technician, supervisor, security, auditor, admin @sentinelops.test · password SentinelOps!2026</small></section></div></>;
}
